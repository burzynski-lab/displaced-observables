# pixi
.pixi/
# outputs
data/
results/figures/
# python
__pycache__/
*.egg-info/
.pytest_cache/
# latex build
paper/main.pdf
paper/*.log
paper/*.xdv
paper/*.aux
.DS_Store

# data/ (with slash) does not match the cluster symlink, so ignore both
data
# job manifests are regenerated by slurm/manifest.sh
slurm/manifest_*.txt

# working dirs are symlinks to /ourdisk bulk storage
logs
models
results
